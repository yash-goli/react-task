version: "3.8"

services:
  app:
    container_name: auto1-task-dev
    image: auto1-task-dev
    build:
      context: .
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 3000:3000